# ===========================================
# Nuxt App - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# Cloudflare Configuration (for production)
# ===========================================
# These are set automatically when deploying to Cloudflare Pages
# Only needed if using remote bindings for local development

# Cloudflare Account ID (optional - for remote bindings)
# CLOUDFLARE_ACCOUNT_ID=

# Cloudflare API Token (optional - for remote bindings)
# CLOUDFLARE_API_TOKEN=

# ===========================================
# Database Configuration
# ===========================================
# For local development, NuxtHub uses libsql with .data/db/
# For production, D1 bindings are configured in wrangler.jsonc

# Optional: Override database URL for external database
# DATABASE_URL=

# ===========================================
# Email (Zeptomail)
# ===========================================
# Get your API key from: https://www.zoho.com/zeptomail/
# Note: Uses HTTP API (Cloudflare Workers compatible, not SMTP)
ZEPTOMAIL_API_KEY=your-zeptomail-api-key
MAIL_FROM_ADDRESS=contact@yourdomain.com
MAIL_FROM_NAME=Your App Name

# ===========================================
# Build Configuration
# ===========================================
# Override Nitro preset (default: cloudflare_pages)
# Set to 'node' for local development without Cloudflare bindings
# NITRO_PRESET=node
